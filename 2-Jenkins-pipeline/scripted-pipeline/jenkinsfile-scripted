node {
    stage('Build') {
        // Run build commands
        sh 'echo "Building Scripted Pipeline project..."'
    }
    stage('Test') {
        // Run tests
        sh 'echo "Running Scripted Pipeline tests..."'
    }
    stage('Deploy') {
        sh 'echo "Deploying Scripted Pipeline application..."'
    }
}
